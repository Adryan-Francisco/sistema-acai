services:
  - type: web
    name: sistema-acai-server
    runtime: node
    plan: free
    buildCommand: cd server && npm ci
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
