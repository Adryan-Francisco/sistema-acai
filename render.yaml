services:
  - type: web
    name: sistema-acai-server
    runtime: node
    plan: free
    buildCommand: cd server && npm ci
    startCommand: node server/server.js
    workingDirectory: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
