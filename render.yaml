services:
  - type: web
    name: sistema-acai-server
    runtime: node
    buildCommand: cd server && npm install
    startCommand: bash start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
